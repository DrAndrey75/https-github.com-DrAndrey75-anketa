node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Firebase — реальный конфиг не коммитим, используем firebase-config.example.json
src/firebase-config.json

# Firebase CLI кэш
.firebase/
